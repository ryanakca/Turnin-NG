#!/usr/bin/make -f

include /usr/share/cdbs/1/rules/debhelper.mk

DEB_COMPRESS_EXCLUDE := .py
DEB_INSTALL_MANPAGES_turnin-ng := doc/turnin.1 docs/project.1

build/turnin-ng::
	makeinfo -o $(CURDIR)/debian/usr/share/info/turnin-ng.info $(CURDIR)/doc/turnin-ng.texi

binary-post-install/turnin-ng::
	dh_pysupport
